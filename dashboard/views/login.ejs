<%- include('partials/head', { title: t('login.title') + ' – ' + brandName }) %>

<div class="auth">
  <div class="auth__card auth__card--sm">
    <span class="auth__logo"><%- icon('lock') %></span>
    <h1><%= t('login.title') %></h1>
    <p class="auth__lead"><%= t('login.lead') %></p>
    <a class="btn btn--primary btn--lg" href="/auth/discord"><%- icon('bot') %> <%= t('login.continue') %></a>
    <p class="auth__hint"><a href="/"><%= t('login.back_home') %></a></p>
  </div>
</div>

<%- include('partials/foot') %>
