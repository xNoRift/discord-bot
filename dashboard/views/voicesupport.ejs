<%- include('partials/head', { title: 'Voice Support' }) %>
<%- include('partials/app-shell-open') %>

<div class="page-head"><div class="page-head__row"><h1><%- icon('chat') %> Voice Support</h1></div>
  <p>Benachrichtigung für dein Team, sobald jemand im Sprach-Warteraum auf Hilfe wartet.</p>
</div>

<div class="module-status" id="moduleStatus">
  <span class="module-status__dot"></span>
  <span class="module-status__text"><b id="msTitle">…</b><span id="msText"></span></span>
  <button class="btn btn--ghost btn--sm" id="msToggle">…</button>
</div>

<form id="vsForm" class="card">
  <div class="card__head"><h2><%- icon('chat') %> Voice-Support</h2></div>
  <p class="card__sub">Betritt jemand den Warteraum, pingt der Bot dein Support-Team im gewählten Textkanal – so verpasst niemand eine Anfrage.</p>
  <div class="col-2">
    <div class="field"><label>Warteraum (Sprachkanal)</label><select name="waitingChannelId" data-type="voice"></select></div>
    <div class="field"><label>Benachrichtigungs-Kanal</label><select name="notifyChannelId" data-type="text"></select></div>
  </div>
  <div class="field" style="max-width:420px;"><label>Support-Rolle (wird gepingt)</label><select name="roleId" data-type="role"></select></div>
</form>

<%- include('partials/app-shell-close') %>
<script src="/static/js/voicesupport.js?v=<%= typeof assetVer !== "undefined" ? assetVer : "1" %>"></script>
<%- include('partials/foot') %>
