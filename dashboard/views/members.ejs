<%- include('partials/head', { title: guild.name + ' – Mitglieder' }) %>
<%- include('partials/app-shell-open') %>

<div class="page-head">
  <div class="page-head__row"><h1><%- icon('users') %> Mitglieder <span class="muted">(nur Besitzer)</span></h1></div>
  <p>Nur für dich als Bot-Besitzer: Rollen erstellen, einzelnen Mitgliedern einen Nicknamen geben, Rollen zuweisen/entziehen und Personen zwischen Sprachkanälen verschieben.</p>
</div>

<div class="card" id="permWarn" hidden>
  <div class="card__head"><h2><%- icon('shield') %> Eingeschränkt</h2></div>
  <p class="card__sub" id="permWarnText"></p>
</div>

<div class="card">
  <div class="card__head"><h2><%- icon('plus') %> Rolle erstellen</h2></div>
  <form id="roleCreateForm">
    <div class="col-2">
      <div class="field"><label>Name</label><input id="rcName" maxlength="100" placeholder="z. B. Event-Team" required /></div>
      <div class="field">
        <label>Farbe</label>
        <div class="row-inline">
          <input type="color" id="rcColor" value="#7c5cff" style="max-width:70px;" />
          <input id="rcColorText" placeholder="#7c5cff (leer = keine)" maxlength="7" />
        </div>
      </div>
    </div>
    <div class="setting-row">
      <div class="setting-row__text"><b>Getrennt anzeigen</b><span>Mitglieder mit dieser Rolle stehen in der Mitgliederliste separat oben</span></div>
      <label class="toggle"><input type="checkbox" id="rcHoist"><span class="toggle__track"></span></label>
    </div>
    <div class="setting-row">
      <div class="setting-row__text"><b>Erwähnbar</b><span>Jeder darf die Rolle mit @ anpingen</span></div>
      <label class="toggle"><input type="checkbox" id="rcMentionable"><span class="toggle__track"></span></label>
    </div>
    <div class="setting-row">
      <div class="setting-row__text"><b>Administrator-Rechte</b><span style="color:var(--danger,#f0616d)">⚠️ Volle Rechte auf dem Server – nur wenn du sicher bist</span></div>
      <label class="toggle"><input type="checkbox" id="rcAdmin"><span class="toggle__track"></span></label>
    </div>
    <div style="margin-top:12px;"><button class="btn btn--primary btn--sm" type="submit"><%- icon('plus', 'icon--sm') %> Rolle erstellen</button> <span class="muted" id="rcMsg"></span></div>
  </form>
</div>

<div class="card">
  <div class="card__head"><h2><%- icon('server') %> Mitglied suchen</h2></div>
  <div class="field">
    <label>Name oder Benutzer-ID</label>
    <input id="mSearch" type="search" placeholder="z. B. Max  oder  123456789012345678" autocomplete="off" />
    <small>Tippe einen Namen (Anfang) oder füge eine Benutzer-ID ein.</small>
  </div>
  <div id="mResults"><p class="muted">Suche oben nach einem Mitglied.</p></div>
</div>

<div class="card" id="mEditor" hidden>
  <div class="card__head"><h2><%- icon('edit') %> <span id="edName">Mitglied</span></h2></div>

  <div class="mmember" id="edHeader"></div>

  <form id="memberForm">
    <div class="field" style="margin-top:14px;">
      <label>Nickname</label>
      <input id="edNick" name="nickname" maxlength="32" placeholder="(leer = kein Nickname)" />
      <small id="edNickHint"></small>
    </div>

    <div class="field" style="margin-top:8px;">
      <label>Rollen</label>
      <div id="edRoles"><div class="loading">Lädt…</div></div>
      <small class="muted" id="edRolesMsg"></small>
    </div>
    <p class="muted" style="margin-top:6px;">Änderungen an Nickname und Rollen speicherst du unten mit der Leiste.</p>
  </form>

  <div class="field" style="margin-top:8px;">
    <label>Sprachkanal</label>
    <p class="muted" id="edVoiceState" style="margin:2px 0 8px;"></p>
    <div class="row-inline">
      <select id="edVoiceTarget"></select>
      <button class="btn btn--primary btn--sm" id="edVoiceMove"><%- icon('send', 'icon--sm') %> Verschieben</button>
      <button class="btn btn--outline btn--sm" id="edVoiceKick">Aus Voice trennen</button>
    </div>
    <small id="edVoiceHint"></small>
  </div>
</div>

<style>
  .mmember { display:flex; align-items:center; gap:12px; }
  .mmember img { width:52px; height:52px; border-radius:50%; }
  .mmember b { font-size:1.05rem; }
  .mrole { display:flex; align-items:center; gap:9px; padding:7px 4px; border-bottom:1px solid var(--line); cursor:pointer; }
  .mrole:last-child { border-bottom:0; }
  .mrole input { width:17px; height:17px; accent-color:var(--accent); }
  .mrole.is-locked { opacity:.5; cursor:not-allowed; }
  .mrole__dot { width:10px; height:10px; border-radius:50%; flex:none; }
  .mrole__lock { margin-left:auto; font-size:.75rem; color:var(--muted); }
  .mres { display:flex; align-items:center; gap:10px; padding:8px 4px; border-bottom:1px solid var(--line); }
  .mres:last-child { border-bottom:0; }
  .mres img { width:36px; height:36px; border-radius:50%; }
  .mres .spacer { flex:1; }
</style>

<%- include('partials/app-shell-close') %>
<script src="/static/js/members.js?v=<%= typeof assetVer !== "undefined" ? assetVer : "1" %>"></script>
<%- include('partials/foot') %>
